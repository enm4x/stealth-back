
// const encoder = new TextEncoder();

// async function connect(address: string) {
//     const conn4 = await Deno.connect({ hostname: address, port: 8080, transport: "tcp" });
//     conn4.close();
// }

// async function sendMessage(message: string, server: Deno.Conn){
//     await server.write(encoder.encode(message))
// }

// connect("localhost")

